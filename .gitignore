# dependencias
/node_modules
/.pnp
.pnp.*

# build
/.next/
/out/
/build
next-env.d.ts

# Prisma Client generado
/lib/db/generated

# entorno
.env
.env*.local

# testing y varios
/coverage
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo

# Claude: el directorio queda afuera, menos los skills del proyecto.
# Hay que ignorar el contenido y no el directorio, porque git no desciende
# en un directorio ignorado y la excepción nunca se llegaría a evaluar.
/.claude/*
!/.claude/skills/

# Notas de trabajo pendiente, locales
PENDIENTES.md

# Los reescribe `next dev` en cada directorio donde corre
AGENTS.md
CLAUDE.md
